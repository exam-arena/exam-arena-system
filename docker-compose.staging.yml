services:
  backend:
    environment:
      REDIS_URL: redis://redis:6379/0
    depends_on:
      redis:
        condition: service_started

  redis:
    image: redis:7
    ports:
      - "6379:6379"
    restart: unless-stopped
